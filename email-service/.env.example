PORT=3001

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=

EMAIL_FROM="VSIT Student Assistant <no-reply@example.com>"

# Use 'mock' to prevent real emails during tests/dev
EMAIL_PROVIDER_MODE=smtp
